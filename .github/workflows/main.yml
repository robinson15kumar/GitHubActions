name : myWorkflowPractice
on: push

# env:
#   first_name: Ram

jobs:
  myfirstjob:
    # env:
    #   last_name: Shyam
    
    name: my first job
    runs-on: ubuntu-latest
    steps:
    - name: checkout
      uses: actions/checkout@v6.0.3
    
    - name: first task
      run: echo "${{vars.first_name}} and ${{secrets.last_name}}"
      
    - name: list file
      run: ls -lart
      
