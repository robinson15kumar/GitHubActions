name : myWorkflowPractice
on: workflow_dispatch

jobs:
  myfirstjob:
    name: my first job
    runs-on: ubuntu-latest
    steps:
    - name: checkout
      uses: actions/checkout@v6.0.3
     
    - name: first task
      run: echo "Hello World!"
    - name: list file
      run: ls -lart
      
